# ChatBridge UI Components

React components for rendering ChatBridge app panels in the UI.

## Files

- `SidePanel.tsx` — Side panel component that hosts app iframes and manages their visibility

## Testing

Tests are in `__tests__/` subdirectory.
